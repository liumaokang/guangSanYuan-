<view class="title">可用门店</view>
<view class="store-item" hidden="{{storeList.length===0}}" wx:for="{{storeList}}" wx:key="{{item.id}}">{{item.name}}</view>
<view class="all" hidden="{{storeList.length!==0}}">全部门店可用</view>
